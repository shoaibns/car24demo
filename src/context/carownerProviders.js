"use client"; // Explicitly mark this as a client component

import { CarOwnersContextProvider } from "./CarOwnersContext";

export default function Providers({ children }) {
  //   return <CarOwnersContextProvider>{children}</CarOwnersContextProvider>;
}

// Delete this file
